{
  "name": "electron-desktop-gadgets",
  "version": "0.0.1",
  "main": "main.js",
  "gypfile": true,
  "scripts": {
    "start": "electron .",
    "electron-rebuild": "./node_modules/.bin/electron-rebuild -f -w electron-desktop-gadgets",
    "compile": "node-gyp rebuild & npm run electron-rebuild",
    "compile-and-start": "npm run compile && npm start"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-gyp": "^5.1.1"
  },
  "devDependencies": {
    "electron": "^9.0.1",
    "electron-rebuild": "^1.11.0"
  }
}
